#include <ros/ros.h>
#include <iostream>

#include "path_searching/dyn_a_star.h"

// using std;

int main(int argc, char **argv)
{
    // ros::init(argc, argv, "path_searching_test");
    // ros::NodeHandle nh("~");
    // ROS_INFO("A star search start!.");
    // GridMap::Ptr grid_map_;
    // grid_map_.reset(new GridMap);
    // grid_map_->initMap(nh);
    // AStar a_star;
    // // a_star.setVisualizer(std::make_shared<visualization::Visualization>(nh));
    // a_star.initGridMap(grid_map_, Eigen::Vector3i(200, 200, 100));
    // Eigen::Vector3d in(0, 0, 1);
    // Eigen::Vector3d out(10, 10, 1);
    // vector<Eigen::Vector3d> a_star_path;
    // ASTAR_RET ret = a_star.AstarSearch(grid_map_->getResolution(), in, out);
    // if(ret == ASTAR_RET::SUCCESS)
    // {
    //     ROS_INFO("A star search success!.");
    //     // a_star_path = a_star->getPath();
    // }
    // ROS_INFO("A star search finished!");
}